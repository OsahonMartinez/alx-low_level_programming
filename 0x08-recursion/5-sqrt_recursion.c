#include "main.h"
/**
 * _sqrt_recursion - returns the natural squareroot of a number.
 * @n: the number
 * Return: squareoot of n
 * -1 if n does not have a natural sqrt
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);
}
