#include <stdio.h>
/**
 * main - Entry point
 * Return :Always 0 (success/correct)
 */
int main(void)
{
	char alpa;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}

	putchat('\n');

	return (0);
}

